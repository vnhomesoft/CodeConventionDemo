// See https://aka.ms/new-console-template for more information
using CodeConventionDemo;

Console.WriteLine( "Hello, World!" );

var shop = new Shop();
if (shop != null)
{
    Console.WriteLine( shop.DisplayName );
}